
const path = '/assets/images'

const iconPath = {
    // developer: `${path}/developer.jpg`,
    google: `${path}/google.svg`,
    stripe: `${path}/stripe.svg`,
    xero: `${path}/xero.svg`,
}

export default iconPath;
